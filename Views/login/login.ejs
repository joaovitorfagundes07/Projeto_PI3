<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/assets/vendor/bootstrap-icons/bootstrap-icons.css">
  <style>
    body {
      min-height: 100vh;
      /* Gradiente verde para agro */
      background: linear-gradient(135deg, #22c55e 0%, #0f172a 100%);
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-card {
      background: #0b1220ee;
      border-radius: 24px;
      box-shadow: 0 10px 30px rgba(0,0,0,.35);
      padding: 32px 28px;
      max-width: 400px;
      width: 100%;
      color: #e5e7eb;
    }
    .logo {
      width: 48px; height: 48px; border-radius: 12px;
      background: linear-gradient(135deg, #22c55e, #10b981);
      color: #052e16; font-weight: 800; display: grid; place-items: center;
      font-size: 1.5rem; margin-bottom: 12px;
    }
    .form-label { color: #cbd5e1; }
    .form-control { background: #0a1221; border: 1px solid #94a3b840; color: #e5e7eb; }
    .form-control:focus { border-color: #86efac; box-shadow: 0 0 0 4px #86efac30; }
    .btn-primary {
      background: linear-gradient(180deg, #22c55e, #16a34a);
      border: none; color: #052e16; font-weight: 700;
      border-radius: 14px; width: 100%;
      box-shadow: 0 6px 18px rgba(34,197,94,.35);
    }
    .btn-primary:hover { filter: brightness(1.05); }
    .footer { text-align: center; color: #94a3b8; margin-top: 16px; }
    .link { color: #86efac; text-decoration: none; }
    .link:hover { text-decoration: underline; }
  </style>
</head>
<body>
  <div class="login-card mx-auto">
    <div class="text-center mb-3">

      <h1 class="h4 mb-1" id="title">Bem-vindo de volta</h1>
      <p class="mb-3" style="color:#94a3b8;">Entre para continuar</p>
    </div>
    <form method="post" action="/login">
      <div class="mb-3">
        <label for="email" class="form-label">E-mail</label>
        <input type="email" class="form-control" id="email" name="email" placeholder="voce@exemplo.com" required autocomplete="username">
      </div>
      <div class="mb-3">
        <label for="password" class="form-label">Senha</label>
        <input type="password" class="form-control" id="password" name="senha" placeholder="Sua senha" required minlength="6" autocomplete="current-password">
      </div>
      <% if (typeof msg !== 'undefined' && msg) { %>
        <div class="alert alert-danger py-2"><%= msg %></div>
      <% } %>
      <button type="submit" class="btn btn-primary mt-2">Entrar</button>
    </form>

  </div>
</body>
</html>
