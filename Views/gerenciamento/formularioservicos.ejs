<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title><%= servico ? 'Editar Serviço' : 'Cadastro de Serviço' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #fff9e6; /* Amarelado suave */
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .form-card {
            background: #ffffff;
            border-radius: 15px;
            padding: 40px 30px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
            max-width: 450px;
            width: 100%;
            position: relative;
        }
        .form-card h2 {
            margin-bottom: 30px;
            font-weight: 600;
            color: #a67c00; /* Marrom/ocre escuro para contraste */
            text-align: center;
        }
        .form-label {
            font-weight: 500;
            color: #a67c00;
        }
        .btn-primary {
            background-color: #f0ad4e; /* Laranja/amarelado */
            border-color: #f0ad4e;
            font-weight: 500;
        }
        .btn-primary:hover {
            background-color: #ec9c3b;
            border-color: #d4882f;
        }
        .btn-voltar {
            position: absolute;
            top: 15px;
            right: 15px;
        }
    </style>
</head>
<body>
    <div class="form-card">
        <a href="/servicos" class="btn btn-danger btn-sm btn-voltar">Voltar</a>
        <h2><%= servico ? 'Editar Serviço' : 'Cadastro de Serviço' %></h2>

        <form id="formserv">
            <input type="hidden" id="id" value="<%= servico ? servico.id : '' %>">

            <div class="mb-3">
                <label for="nome" class="form-label">Nome do Serviço</label>
                <input type="text" class="form-control" id="nome" name="nome" required 
                       value="<%= servico ? servico.nome : '' %>">
            </div>

            <div class="mb-3">
                <label for="descricao" class="form-label">Descrição</label>
                <input type="text" class="form-control" id="descricao" name="descricao" required 
                       value="<%= servico ? servico.descricao : '' %>">
            </div>

            <div class="d-grid mt-4">
                <button type="submit" class="btn btn-primary btn-lg">
                    <%= servico ? 'Atualizar Serviço' : 'Cadastrar Serviço' %>
                </button>
            </div>
        </form>
        <div id="msg" class="mt-2"></div>
    </div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/assets/js/servico/cadastra.js"></script>
</body>
</html>
